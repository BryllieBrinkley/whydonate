import SwiftUI

struct HistoryView: View {
    var body: some View {
        NavigationStack {
            Text("Your past donations will show here.")
                .navigationTitle("History")
        }
    }
}
